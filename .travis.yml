language: php

php:
  - 5.5
  - 5.6
  - 7.0
  - hhvm
  - hhvm-nightly

before_install:
  - composer self-update

install:
  - composer update --prefer-dist

script:
  - ./vendor/bin/phpunit

matrix:
  fast_finish: true
  allow_failures:
    - php: hhvm-nightly
